export const meta = () => {
  return [
    { title: "category" },
    { name: "description", content: "welcome category" },
  ];
};

const Category = () => {
  return <h1>category</h1>;
};

export default Category;
